{
  "name": "cs16.css",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "browserslist": [
    "chrome >= 109",
    "firefox >= 115",
    ">= 0.25%"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "pnpm build && cp dist/cs16.min.css preview/ && cp src/index.css preview/ && vite preview --outDir preview",
    "lint": "stylelint \"src/*.css\"",
    "lint:fix": "stylelint \"src/*.css\" --fix"
  },
  "packageManager": "pnpm@11.8.0",
  "devDependencies": {
    "browserslist": "^4.28.2",
    "lightningcss": "^1.32.0",
    "stylelint": "^17.13.0",
    "stylelint-config-standard": "^40.0.0",
    "vite": "^8.0.16"
  }
}
